# Ignore build outputs from performing a nix-build or `nix build` command
result
result-*

# Ignore automatically generated direnv output
.direnv

# Flakes: DO commit flake.lock for reproducibility!
# But ignore temporary flake files
.nix-*

# Ignore instructions files
.github/instructions/*.md

# Sensitive configuration files
lie_*.nix
modules/includes/variables.nix
modules/includes/dnscrypt-configs/dnscrypt-proxy-*.toml
.scripts/find_*
.scripts/check_*

# SOPS encrypted secrets (safe to commit when encrypted!)
# secrets/secrets.yaml is now ENCRYPTED and safe to commit
secrets/*.yaml
!secrets/*.yaml.example
!secrets/secrets.yaml
# Age keys (if stored locally)
**/.sops/
**/sops/age/keys.txt

# Backup files
*.bak

# Ignore VSCode settings
.vscode/*
